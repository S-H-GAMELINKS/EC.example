<div class="row mt-3">
<% @products.each do |product| %>
    <div class="card col-12 m-2">
      <div class="card-body">
            <h5 class="card-title"><%= product.product.title %></h5>
            <p class="card-text"><%= truncate(product.product.description) %></p>
            <p class="card-text">Count: <%= product.quantity %></p>
      </div>
    </div>
<% end %>
</div>